# 한화내일아카데미 출석 시스템 명세서

- 문서 버전: v1.0
- 작성일: 2026-08-31
- 작성 목적: GitHub Copilot Agent 모드를 활용한 개발 착수용 기능/기술 명세

---

## 1. 개요

한화내일아카데미는 별도의 출결 관리 시스템이 없어, 교육생이 QR코드를 스캔하여 간단히 출석을 제출하고 운영진이 이를 확인·다운로드할 수 있는 웹 기반 출석 시스템을 구축한다.

- 저장소: GitHub (정적 사이트로 배포)
- 백엔드/DB: Firebase (Firestore + Hosting)
- 개발 방식: GitHub Copilot Agent 모드
- 운영 규모: 3개 반

---

## 2. 사용자 및 역할

| 역할 | 설명 |
|---|---|
| 교육생 | QR코드를 스캔하여 출석 페이지에 접속, 이름과 반을 선택해 출석 제출 |
| 운영진(관리자) | 강의실별 QR코드를 생성/출력, 출석 현황 조회 및 엑셀 다운로드 |

---

## 3. 핵심 기능 요구사항

### 3.1 출석 제출 (교육생용)
- 이름 입력 (텍스트 입력)
- 반 선택 (드롭다운: A반 / B반 / C반 — 값은 설정에서 쉽게 변경 가능하도록 구현)
- "출석하기" 버튼 클릭 시 아래 정보를 Firestore에 저장
  - 이름
  - 선택한 반
  - 제출 시각 (서버 타임스탬프 기준, 클라이언트 시간 조작 방지)
- 제출 완료 시 화면에 확인 메시지 표시 (예: "OOO님, A반 출석이 완료되었습니다.")
- 중복 제출 방지(선택사항, 아래 4.4 참고)

### 3.2 QR코드 생성 (관리자용)
- 관리자가 반/날짜를 선택하면 해당 조건이 담긴 출석 제출 URL을 생성
  - 예: `https://[사이트주소]/attend.html?class=A&date=2026-08-31`
- 위 URL을 QR코드 이미지로 변환하여 화면에 표시 (라이브러리 사용, 예: qrcode.js)
- QR코드 다운로드(이미지 저장) 또는 인쇄 기능 제공
- 교육생은 URL 파라미터 덕분에 반을 자동으로 선택된 상태로 보게 되어, 이름만 입력하면 되도록 UX 단순화 (반 선택은 수정 가능하도록 유지)

### 3.3 출석 현황 조회 및 엑셀 다운로드 (관리자용)
- 날짜별/반별 필터로 출석 목록 조회 (표 형태: 이름 / 반 / 제출 시각)
- "엑셀 다운로드" 버튼 클릭 시 현재 조회된 목록을 `.xlsx` 파일로 다운로드
  - 라이브러리: SheetJS(xlsx.js) 활용
  - 파일명 예시: `출석부_A반_2026-08-31.xlsx`
- 전체 데이터 다운로드 옵션도 별도 제공 (반/날짜 필터 없이 전체)

### 3.4 관리자 인증
- 관리자 페이지(QR 생성, 출석 현황 조회)는 별도 보호 필요
- 1차 구현: 간단한 관리자 비밀번호(고정값 또는 환경설정) + 세션 유지
- 확장 옵션: Firebase Authentication(이메일/비밀번호) 적용

---

## 4. 비기능 요구사항

### 4.1 반응형 디자인
- 교육생은 대부분 스마트폰으로 QR 스캔 후 접속하므로 모바일 우선(Mobile-first) 디자인 필수
- 관리자 페이지는 데스크톱에서도 표 형태가 잘 보이도록 구성

### 4.2 성능
- 정적 페이지 + Firebase 클라이언트 SDK 사용으로 별도 서버 없이 동작
- Firestore 읽기/쓰기 최소화 (실시간 리스너보다는 필요 시점에 조회하는 방식 권장)

### 4.3 보안
- Firestore 보안 규칙(Security Rules) 설정
  - 출석 제출(쓰기)은 누구나 가능하되, 필드 형식 검증(이름 비어있지 않음, 반 값이 허용된 3개 중 하나인지 등) 적용
  - 출석 데이터 읽기(조회)는 관리자만 가능하도록 제한 (관리자 인증과 연동)
- API 키 등 민감 정보는 `.env` 또는 별도 설정 파일로 분리, `.gitignore`에 등록

### 4.4 중복 제출 방지 (선택 기능)
- 동일 브라우저에서 같은 날짜/반에 중복 제출 시 로컬스토리지 기반으로 안내 문구 표시
- 완벽한 부정 방지는 아니며, 단순 실수 방지 목적

---

## 5. 데이터 모델 (Firestore)

### 5.1 컬렉션: `attendances`

| 필드명 | 타입 | 설명 |
|---|---|---|
| `name` | string | 교육생 이름 |
| `class` | string | 반 (`A`, `B`, `C`) |
| `submittedAt` | timestamp | 서버 타임스탬프 (Firestore `serverTimestamp()`) |
| `date` | string | 출석 기준 날짜 (`YYYY-MM-DD`, QR 파라미터 또는 서버 시각 기준 자동 생성) |

### 5.2 컬렉션: `classes` (설정용, 선택사항)

| 필드명 | 타입 | 설명 |
|---|---|---|
| `id` | string | 반 코드 (A/B/C) |
| `label` | string | 화면에 표시할 반 이름 (예: "A반") |

> 반 목록을 코드에 하드코딩할지, `classes` 컬렉션으로 관리할지는 초기 구현에서는 하드코딩으로 시작하고 추후 확장 가능하도록 설계 권장.

---

## 6. 화면 구성

### 6.1 출석 제출 페이지 (`attend.html`)
- 상단: 아카데미 로고/타이틀
- 이름 입력창
- 반 선택 드롭다운 (URL 파라미터 있으면 자동 선택)
- [출석하기] 버튼
- 제출 후 완료 메시지 영역

### 6.2 관리자 로그인 페이지 (`admin-login.html`)
- 비밀번호 입력 후 관리자 대시보드로 이동

### 6.3 관리자 대시보드 (`admin.html`)
- 탭 또는 섹션 구분
  1. **QR 생성**: 반 선택 + 날짜 선택 → QR코드 표시 → 다운로드/인쇄
  2. **출석 현황**: 날짜/반 필터 → 표 형태 목록 → 엑셀 다운로드 버튼

---

## 7. 기술 스택

| 영역 | 기술 |
|---|---|
| 프론트엔드 | HTML, CSS, Vanilla JavaScript (빌드 도구 없이 단순 구성) |
| 백엔드/DB | Firebase Firestore |
| 호스팅 | Firebase Hosting 또는 GitHub Pages |
| QR 생성 | qrcode.js (또는 유사 클라이언트 라이브러리) |
| 엑셀 다운로드 | SheetJS (xlsx) |
| 버전 관리 | GitHub |
| 개발 도구 | GitHub Copilot Agent 모드 |

---

## 8. 프로젝트 구조(예시)

```
hanwha-academy-attendance/
├── index.html            # 랜딩 또는 attend.html로 리다이렉트
├── attend.html           # 교육생 출석 제출 페이지
├── admin-login.html      # 관리자 로그인
├── admin.html            # 관리자 대시보드 (QR 생성 + 출석 현황)
├── css/
│   └── style.css
├── js/
│   ├── firebase-config.js   # Firebase 설정 (실제 키는 gitignore 처리)
│   ├── attend.js
│   ├── admin.js
│   └── excel-export.js
├── .gitignore
└── README.md
```

---

## 9. 개발 단계 제안

1. Firebase 프로젝트 생성 및 Firestore/Hosting 설정
2. `attend.html` + 출석 제출 로직 (Firestore 쓰기) 구현
3. 관리자 인증(간단한 비밀번호) 구현
4. 출석 현황 조회 + 표 렌더링 구현
5. 엑셀 다운로드 기능 연동
6. QR코드 생성 기능 연동
7. Firestore 보안 규칙 작성 및 테스트
8. 반응형 스타일링
9. Firebase Hosting 또는 GitHub Pages 배포

---

## 10. 향후 확장 가능성 (참고용, 이번 범위 아님)

- Firebase Authentication으로 관리자 계정 다중 관리
- 지각/결석 자동 판정 (제출 시각 기준)
- 교육생별 누적 출석률 통계
- Slack/이메일 알림 연동
